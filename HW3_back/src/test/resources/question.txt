1. Если из ответа требуется вытащить несколько значений, то это делается через переменную типа response?
или есть еще методы?
2. Что если мне надо выполнять после почти каждого теста одинаковый тирдавн (удаление картинки), но нельзя его оформить как
@AfterAll, т.к после некоторых тестов мне не надо удалять картинку (например для случая добавления в избранное или
просто запроса информации про картинку запросом get) как будут выглядеть методы @After - Можно ли вынести его в
отдельную функцию? или придется каждый раз писать что то вроде @After void teardown1 {...},
после следующего теста @After void teardown2 {...} итд
3. Как можно запустить только 2 теста ? например загрузку картинки и следом за ним добавление в избранное ?
Если выполнить их отдельно, то id картинки уже не будет лежать в переменной при запуске второго теста

//к дз4

можно ли параметризивароть спецификации:
например 
multiPartSpecificationWithFile = new MultiPartSpecBuilder(new File("src/test/resources/test_img.jpg"))
                .controlName("image")
                .build();
можно ли передавать и как разные значения для .controlName ?